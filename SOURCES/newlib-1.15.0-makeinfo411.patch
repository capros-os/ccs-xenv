diff -ruN baseline-newlib-1.15.0/configure newlib-1.15.0/configure
--- baseline-newlib-1.15.0/configure	2007-11-13 01:16:07.000000000 -0500
+++ newlib-1.15.0/configure	2007-11-13 01:18:31.000000000 -0500
@@ -6137,7 +6137,7 @@
     # For an installed makeinfo, we require it to be from texinfo 4.4 or
     # higher, else we use the "missing" dummy.
     if ${MAKEINFO} --version \
-       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|[5-9])' >/dev/null 2>&1; then
+       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|4\.[1-9][0-9]|[5-9])' >/dev/null 2>&1; then
       :
     else
       MAKEINFO="$MISSING makeinfo"
diff -ruN baseline-newlib-1.15.0/configure.ac newlib-1.15.0/configure.ac
--- baseline-newlib-1.15.0/configure.ac	2007-11-13 01:16:07.000000000 -0500
+++ newlib-1.15.0/configure.ac	2007-11-13 01:18:34.000000000 -0500
@@ -2412,7 +2412,7 @@
     # For an installed makeinfo, we require it to be from texinfo 4.4 or
     # higher, else we use the "missing" dummy.
     if ${MAKEINFO} --version \
-       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|[5-9])' >/dev/null 2>&1; then
+       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|4\.[1-9][0-9]|[5-9])' >/dev/null 2>&1; then
       :
     else
       MAKEINFO="$MISSING makeinfo"
